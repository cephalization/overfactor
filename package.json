{
  "name": "overfactor-scaffold",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp fmt && vp lint && vp run -F '!just-git' test && vp run -r build",
    "dev": "vp run -F '@overfactor/daemon...' build && concurrently -k -n sdk,shared,claude,pi,daemon-build,daemon,app -c auto 'vp run @overfactor/sdk#dev' 'vp run @overfactor/integration-utils#dev' 'vp run @overfactor/integration-claude-code#dev' 'vp run @overfactor/integration-pi#dev' 'vp run @overfactor/daemon#dev' 'node scripts/dev-daemon.mjs' 'vp run @overfactor/desktop#dev'",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@overfactor/daemon": "workspace:*",
    "@oxlint/plugins": "catalog:",
    "@typescript/native-preview": "^7.0.0-dev.20260707.2",
    "concurrently": "catalog:",
    "oxlint": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.22.0"
}
