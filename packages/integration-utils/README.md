# @overfactor/integration-utils

Shared implementation for Overfactor agent integrations: guided-review prompt construction, diff budgeting, structured-output validation/retry, and bounded CLI execution.

Harness-specific invocation and output envelopes remain in each integration package.
